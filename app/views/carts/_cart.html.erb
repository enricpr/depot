<h2>Your Cart</h2>
<div class="posts">
  <%= render(cart.line_items) %>
  <div class="post">
    <div class="post-content">
	  Total:
	</div>
    <span class="add2Cart"><%= number_to_currency(cart.total_price) %></span>
  </div>
</div>
<%= button_to "Checkout", new_order_path, method: :get, class: "btn btn-primary btn-sm" %>
<%= button_to 'Empty Cart', cart, method: :delete, data: { confirm: 'fo real?!' }, class: "btn btn-primary btn-sm" %>
